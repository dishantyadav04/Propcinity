# Next.js
- Use `proxy.ts` not `middleware.ts` for middleware files — Next.js 16+ (Turbopack) deprecates the "middleware" convention in favor of "proxy". Confidence: 0.70

# Workflow
- Tasks are specified in `prdv{N}.md` files — read and implement all instructions from the referenced spec file with zero errors and clean `tsc --noEmit` build. Confidence: 0.80
